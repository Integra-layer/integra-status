{
  "name": "integra-status",
  "version": "1.0.0",
  "description": "Integra Infrastructure Status Page",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step'",
    "start": "vercel dev"
  }
}
